import React from "react";
import Tilt from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>Overview.</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-4 text-secondary text-[17px] max-w-3xl leading-[30px]'
      >
I am currently pursuing a Master's degree in Data Science and Artificial Intelligence at the University of Waterloo. My primary objective is to synergize my expertise in data science and machine learning to refine my proficiency in various data science and analytics techniques. Through this, I aim to emerge as a skilled data scientist, well-equipped to tackle intricate challenges professionally within the industry.

Having already explored diverse topics in data science and machine learning during my Bachelor's degree, I am highly motivated to further immerse myself in these fields. My aspiration is to deepen my comprehension of the higher-level conceptual, theoretical, and practical foundations of tools in these domains. Such an endeavour not only promises to enrich my knowledge and insights but also to fuel my professional growth as I advance in my career.

One of my key attributes is my rapid grasp of new concepts, allowing me to collaborate closely with clients to devise efficient, scalable, and user-friendly solutions that effectively address real-world problems. I am enthusiastic about the prospect of joining forces to bring your ideas to fruition and make a positive impact together!
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
